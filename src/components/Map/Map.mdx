---
name: Map
menu: Components
---

import { Playground, Props } from 'docz';
import Map from './index.web.tsx';

# Map

This component is a wrapper to Map, must use code for react and react native,
because the library to mapbox on react and react native is different.

## Properties

<Props of={Map} />

## Basic usage

<Playground>
  <div style={{ width: '100%', height: 300 }}>
    <Map
      defaultLatitude={-12.9704}
      defaultLongitude={-38.5124}
      defaultZoom={6}
      mapStyle="mapbox://styles/mapbox/dark-v9"
    />
  </div>
</Playground>
